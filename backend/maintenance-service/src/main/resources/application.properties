# Application name
spring.application.name=${APP_NAME:maintenance-service}

# Server port
server.port=${SERVER_PORT:8085}

# MongoDB connection from .env file
spring.data.mongodb.uri=${MONGODB_URI}

# Connection pool settings
spring.data.mongodb.auto-index-creation=true

# Logging
logging.level.org.springframework.data.mongodb.core.MongoTemplate=DEBUG
logging.level.org.mongodb.driver=DEBUG


# =====================================================
# Service URLs for inter-service communication
# =====================================================

# API Gateway URL (for Docker environment)
api.gateway.url=${API_GATEWAY_URL:http://localhost:8080}

# Notification Service
notification.service.url=${NOTIFICATION_SERVICE_URL:http://localhost:8083/api/notifications}

# Vehicle Service
vehicle.service.url=${VEHICLE_SERVICE_URL:http://localhost:8081/api/vehicles}

# Payment Service
payment.service.url=${PAYMENT_SERVICE_URL:http://localhost:8082/api/payments}