# Test Profile Configuration
spring.application.name=maintenance-service-test

# Use embedded MongoDB for testing or mock it
spring.data.mongodb.uri=mongodb://localhost:27017/maintenance-test-db

# Disable logging in tests
logging.level.root=WARN
logging.level.com.vehiclerental=INFO

# Server configuration
server.port=0

# Disable swagger in tests
springdoc.swagger-ui.enabled=false
