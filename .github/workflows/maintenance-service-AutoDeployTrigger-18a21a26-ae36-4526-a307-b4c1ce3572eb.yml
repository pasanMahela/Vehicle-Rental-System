name: Trigger auto deployment for maintenance-service

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/maintenance-service-AutoDeployTrigger-18a21a26-ae36-4526-a307-b4c1ce3572eb.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.MAINTENANCESERVICE_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.MAINTENANCESERVICE_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.MAINTENANCESERVICE_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          _targetLabelKey_: _targetLabel_
          registryUrl: orchidacr11.azurecr.io
          registryUsername: ${{ secrets.MAINTENANCESERVICE_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.MAINTENANCESERVICE_REGISTRY_PASSWORD }}
          containerAppName: maintenance-service
          resourceGroup: orchid-rental-rg
          imageToBuild: orchidacr11.azurecr.io/maintenance-service:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


