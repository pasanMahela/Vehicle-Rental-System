name: Trigger auto deployment for booking-service

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/booking-service-AutoDeployTrigger-84f0b7f0-68dc-48a7-934a-7f4b5583a5ed.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.BOOKINGSERVICE_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.BOOKINGSERVICE_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.BOOKINGSERVICE_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          _targetLabelKey_: _targetLabel_
          registryUrl: orchidacr11.azurecr.io
          registryUsername: ${{ secrets.BOOKINGSERVICE_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.BOOKINGSERVICE_REGISTRY_PASSWORD }}
          containerAppName: booking-service
          resourceGroup: orchid-rental-rg
          imageToBuild: orchidacr11.azurecr.io/booking-service:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


