name: Trigger auto deployment for api-gateway

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/api-gateway-AutoDeployTrigger-3a2e92fd-e775-4358-9d5d-527f08904dd6.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.APIGATEWAY_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.APIGATEWAY_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.APIGATEWAY_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          _targetLabelKey_: _targetLabel_
          registryUrl: orchidacr11.azurecr.io
          registryUsername: ${{ secrets.APIGATEWAY_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.APIGATEWAY_REGISTRY_PASSWORD }}
          containerAppName: api-gateway
          resourceGroup: orchid-rental-rg
          imageToBuild: orchidacr11.azurecr.io/api-gateway:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


